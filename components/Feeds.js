import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Feeds = () => {
  return (
    <View>
      <Text>this is the feeds screen</Text>
    </View>
  );
};

export default Feeds;

const styles = StyleSheet.create({});
